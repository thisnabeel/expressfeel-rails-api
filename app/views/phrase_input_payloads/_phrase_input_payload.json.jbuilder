json.extract! phrase_input_payload, :id, :phrase_input_id, :phrase_payload_id, :factory_dynamic_input_id, :created_at, :updated_at
json.url phrase_input_payload_url(phrase_input_payload, format: :json)
